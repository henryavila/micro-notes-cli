# file: deps must be *copied* (not symlinked). A symlink keeps the peer's own
# node_modules/react + node_modules/ink, so Ink's reconciler and blink's
# ThemeProvider load two React copies → invalid hook call / useState of null.
install-links=true
